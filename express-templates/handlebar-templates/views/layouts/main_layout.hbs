<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ pageTitle }}</title>
    {{!-- Cannot include css dynamically as possible in pug --}}
    {{#if mainCSS  }}
        <link rel="stylesheet" href="/css/main.css" />
    {{/if}}
</head>
<body>
    <div>
        <ul>
            <li class="{{#if activeShop}}active{{/if}}">My Shop</li>
            <li class="{{#if activeGuestShop}}active{{/if}}">Guest Shop</li>
        </ul>
    </div>
    {{{ body }}}
</body>
</html>